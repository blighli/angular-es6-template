// add imports of external libs here - will speed up page reload in dev
// as not all the files have to be loaded every time (often hundreds for npm packages)

import 'core-js/shim'; // load all es6 functions and extensions (Array.prototype.find etc.)
import 'text';
//import 'lodash';
//import 'moment';
import 'bluebird';
import 'immutable';
import 'eventemitter2';
import 'loglevel';
import 'autobind-decorator';
import 'angular';
import 'angular-ui-router';
